[user]
    name = yamadapc
    email = tacla.yamada@gmail.com
[core]
    editor = mvim -f -g -v
    excludesfile = ~/.gitignore
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    l = "!source ~/dotfiles/git/helpers.sh && pretty_git_log"
    type = cat-file -t
    dump = cat-file -p
    d = difftool
[diff]
    tool = mvimdiff -f -g -v
[difftool]
    prompt = false
[push]
    default = simple
[merge]
    tool = mvimdiff -f -g -v
